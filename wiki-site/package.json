{
  "name": "wiki-site",
  "version": "0.0.0",
  "packageManager": "pnpm@10.23.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "wiki:validate": "pnpm --filter @workspace/scripts run validate",
    "wiki:sync": "pnpm --filter @workspace/scripts run sync-articles",
    "wiki:sync:dry": "pnpm --filter @workspace/scripts run sync-articles:dry",
    "wiki:convert:discourse": "pnpm --filter @workspace/scripts run convert:discourse",
    "wiki:convert:quora": "pnpm --filter @workspace/scripts run convert:quora",
    "wiki:preview": "PORT=5000 BASE_PATH=/ pnpm --filter @workspace/wiki run dev",
    "wiki:build": "PORT=5000 BASE_PATH=/ pnpm --filter @workspace/wiki run build",
    "wiki:build:pages": "PORT=5000 BASE_PATH=/chargingthefuture/ pnpm --filter @workspace/wiki run build && cp artifacts/wiki/dist/public/index.html artifacts/wiki/dist/public/404.html"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}