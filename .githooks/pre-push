#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

if git rev-parse --verify --quiet "@{upstream}" >/dev/null; then
  bash scripts/check-no-ai-prompts.sh --ref-range "@{upstream}..HEAD"
else
  bash scripts/check-no-ai-prompts.sh --all
fi
