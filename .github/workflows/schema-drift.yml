name: Schema Drift Detection

description: >
  Detects schema drift between Neon DB and ctf/schema.sql. Opens an issue and (optionally) a PR to fix drift.

on:
  schedule:
    - cron: '0 * * * *' # every hour
  workflow_dispatch:
  pull_request:
    paths:
      - 'ctf/schema.sql'

jobs:
  detect-drift:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install schemachange (or sqldef)
        run: |
          pip install schemachange

<<<<<<< HEAD
      - name: Set ENVIRONMENT var (manual or schedule)
        id: set_env
        run: |
          if [ -n "${{ github.event.inputs.environment }}" ]; then
            echo "ENVIRONMENT=${{ github.event.inputs.environment }}" >> $GITHUB_ENV
          else
            echo "ENVIRONMENT=production" >> $GITHUB_ENV
          fi

      - name: Set DATABASE_URL for environment
        id: set_db_url
        run: |
          if [ "$ENVIRONMENT" = "staging" ]; then
            echo "DATABASE_URL=${{ secrets.DATABASE_URL_STAGING }}" >> $GITHUB_ENV
          else
            echo "DATABASE_URL=${{ secrets.DATABASE_URL }}" >> $GITHUB_ENV
          fi

      - name: Validate DATABASE_URL secret
        run: |
          if [ -z "$DATABASE_URL" ]; then
            echo "Missing DATABASE_URL for $ENVIRONMENT environment."
            exit 1
          fi

      - name: Compare Neon DB schema to ctf/schema.sql
=======
      - name: Compare Neon DB schema to ctf/schema.sql
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
>>>>>>> afa5a185 (add schema drift in ci)
        run: |
          # Dump Neon DB schema
          pg_dump --schema-only --no-owner --no-privileges "$DATABASE_URL" > live_schema.sql
          # Compare with ctf/schema.sql
<<<<<<< HEAD
          diff -u ctf/schema.sql live_schema.sql > schema-drift.diff || echo "SCHEMA_DRIFT=1" >> $GITHUB_ENV

      - name: Auto-generate PR with schema.sql fix using Copilot
        if: env.SCHEMA_DRIFT == '1'
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: "fix: sync ctf/schema.sql with Neon DB ($ENVIRONMENT) [auto]"
          title: "fix: sync ctf/schema.sql with Neon DB ($ENVIRONMENT) [auto]"
          body: |
            This PR was auto-generated by the schema drift workflow.
            The following schema drift was detected between Neon DB ($ENVIRONMENT) and ctf/schema.sql:
            
            ```diff
            $(cat schema-drift.diff)
            ```
            
            Please review and merge to resolve schema drift.
          branch: "auto/schema-drift-fix-${{ github.run_id }}"
          add-paths: |
            ctf/schema.sql
            schema-drift.diff
=======
          diff -u ctf/schema.sql live_schema.sql || echo "SCHEMA_DRIFT=1" >> $GITHUB_ENV

      - name: Open issue if drift detected
        if: env.SCHEMA_DRIFT == '1'
        uses: actions/github-script@v7
        with:
          script: |
            github.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: 'Schema drift detected between Neon DB and ctf/schema.sql',
              body: 'Schema drift was detected. Please review the diff and open a PR to resolve.'
            })

      # Optionally: add a step to auto-generate a PR with the fix using Copilot or an AI agent
>>>>>>> afa5a185 (add schema drift in ci)
