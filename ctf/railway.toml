[build]
# Single deploy entry point for Railway service rooted at ctf/packages/web.
buildCommand = "NODE_OPTIONS=--max-old-space-size=4096 npm run build"

[deploy]
startCommand = "npm run check:clerk-env && npm run start"
