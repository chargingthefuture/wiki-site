{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "blog:validate": "pnpm --filter @workspace/scripts run validate",
    "blog:sync": "pnpm --filter @workspace/scripts run sync-articles",
    "blog:sync:dry": "pnpm --filter @workspace/scripts run sync-articles:dry",
    "blog:convert:discourse": "pnpm --filter @workspace/scripts run convert:discourse",
    "blog:convert:quora": "pnpm --filter @workspace/scripts run convert:quora",
    "blog:preview": "PORT=5000 BASE_PATH=/ pnpm --filter @workspace/blog run dev",
    "blog:build": "PORT=5000 BASE_PATH=/ pnpm --filter @workspace/blog run build"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
